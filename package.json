{
  "name": "typing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "git push origin -d gh-pages && npm run build && git add dist -f && git commit -m 'dist' && git subtree push --prefix dist origin gh-pages"
},
  "devDependencies": {
    "typescript": "~5.6.2",
    "vite": "^6.0.5"
  }
}
